node_modules/
dist/
coverage/
*.tsbuildinfo

# The reporter's default sink (log/test_results.jsonl). Telemetry about a local
# run is never a repository artifact — see README.md, "Shipping the run".
/log/
