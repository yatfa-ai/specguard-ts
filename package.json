{
  "name": "specguard-ts",
  "version": "0.0.0",
  "description": "The TypeScript client for SpecGuard: node:test, Vitest, and Jest reporters that ship test-run telemetry.",
  "license": "MIT",
  "homepage": "https://github.com/yatfa-ai/specguard-ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yatfa-ai/specguard-ts.git"
  },
  "bugs": {
    "type": "git",
    "url": "https://github.com/yatfa-ai/specguard-ts/issues"
  },
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "//": "Slices 1-7 ship the runner-agnostic core, the node:test adapter (`./node-test`), the lint command, the Vitest adapter (`./vitest`), the `specguard-ingest` replay bin, and the Jest adapter (`./jest`). Nothing is declared here that does not exist.",
  "exports": {
    ".": "./dist/index.js",
    "./node-test": "./dist/node-test/reporter.js",
    "./vitest": "./dist/vitest/reporter.js",
    "./lint": "./dist/lint/index.js",
    "./jest": "./dist/jest/reporter.js"
  },
  "peerDependencies": {
    "vitest": ">=4.0.0",
    "jest": ">=30.0.0"
  },
  "peerDependenciesMeta": {
    "vitest": {
      "optional": true
    },
    "jest": {
      "optional": true
    }
  },
  "bin": {
    "specguard": "./dist/cli.js",
    "specguard-ingest": "./dist/ingest-cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run build && tsc --project tsconfig.test.json",
    "test": "node --test \".test-build/test/\"",
    "prepare": "npm run build"
  },
  "devDependencies": {},
  "dependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^20.19.0"
  }
}
